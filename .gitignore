knowledge/
content/
.venv/
__pycache__/
*.pyc
.env
.claude/agent-memory/
.claude/projects/
.claude/settings.local.json
.claude/scheduled_tasks.lock
triage-last.json
_deps/
_scratchpad/

# Cloudflare Wrangler tool cache (exposes account id/name + request metadata — never commit)
.wrangler/
cloudflare-worker/.wrangler/

# GitHub notification email exports (clutter; may carry tracking tokens)
*.eml

# ----- leak-prevention patterns (auto-added 2026-04-24) -----
# Block accidental commit of strategy / billing / outreach / pricing files.
# These categories should live in the gitignored hub working directory, never
# in a public repo. Use `git add -f` only after deliberate review.
*_prep.md
*_billing*
*_invoice*
*_outreach*
*_strategy*
*_positioning*
*_pricing*
*_buyer*
*_pitch*
product_direction.md
PORTFOLIO_REFRAME*.md
PLAN-OUTREACH*.md
